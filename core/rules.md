# System Rules

1. **Role isolation** — no cross-directory writes
2. **Zero trust** — all input validated before use
3. **Idempotency** — rerun does not create duplicates
4. **Deterministic output** — strict JSON schema
5. **Failure isolation** — partial failure stops safely
6. **Observability** — every decision logged
